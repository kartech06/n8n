services:
  - type: web
    name: n8n-agent
    env: docker
    plan: free
    envVars:
      - key: N8N_BASIC_AUTH_USER
        value: your-username
      - key: N8N_BASIC_AUTH_PASSWORD
        value: your-password
